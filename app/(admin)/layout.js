'use client';

/**
 * Admin Layout
 *
 * This layout is for admin pages that don't require the standard dashboard authentication.
 * Admin pages handle their own authentication independently.
 */
export default function AdminLayout({ children }) {
  return children;
}
